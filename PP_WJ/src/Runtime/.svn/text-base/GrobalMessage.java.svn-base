package Runtime;

/**
 * 对全局消息的处理，包括 1 全局的上下线信息 2 全局的进出组信息
 * 
 * */
public interface GrobalMessage {
	/**
	 * 上下线事件
	 * */
	void OnGlobalOnline(String sessionid, Boolean flag);

	/**
	 * 进出组事件
	 * */
	void OnGlobalGroupLeave(String sessionid, String groupname, Boolean flag);
}
 